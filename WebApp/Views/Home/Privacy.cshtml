@{
    ViewData["Title"] = App.Resources.Views.UiText.PrivacyPolicy;
}
<h1>@ViewData["Title"]</h1>

<ul>
    @foreach (var claim in User.Claims)
    {
        <li>@claim.Type - @claim.Value</li>
    }
</ul>
