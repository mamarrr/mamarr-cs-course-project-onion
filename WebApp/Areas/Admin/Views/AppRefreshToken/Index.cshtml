@model IEnumerable<App.Domain.Identity.AppRefreshToken>

@{
    ViewData["Title"] = "Index";
}

<h1>@App.Resources.Views.AdminScaffoldText.Index</h1>

<p>
    <a asp-action="Create">@App.Resources.Views.AdminScaffoldText.CreateNew</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RefreshToken)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpirationDT)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PreviousRefreshToken)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PreviousExpirationDT)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AppUser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RefreshToken)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpirationDT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PreviousRefreshToken)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PreviousExpirationDT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AppUser!.FirstName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">@App.Resources.Views.AdminScaffoldText.Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">@App.Resources.Views.AdminScaffoldText.Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">@App.Resources.Views.AdminScaffoldText.Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
