@model WebApp.ViewModels.Management.Tickets.TicketFormPageViewModel
@using App.Resources.Views
@{
    ViewData["Title"] = UiText.ResourceManager.GetString("EditTicket") ?? "Edit ticket";
}

@await Html.PartialAsync("_TicketForm", Model)

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <partial name="_TicketFormScripts" model="Model" />
}
